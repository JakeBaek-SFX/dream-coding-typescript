{
  // JavaScript ES6
  // let
  let name = 'jake';
  name = 'Joe';

  // const
  const age = '1';

  /**
   * JavaScript
   * Primitive: number, string, boolean, bigint, symbol, null, undefined
   * Object: function, array...
   * undefined => values have not been defined yet
   * null => intentional absence of a value
   * unknown => can define any types (ex return type from functions from npm library)
   * any => can define any types
   * void => not returning anything
   * never => (ex function throwError(message: string): never {throw new Error(message)})
   * object => can define any object type (including array)
   */
}