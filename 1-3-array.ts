// Array
const fruits: string[] = ['apple', 'banana'];
const scores: Array<string> = ['1', '2'];

const returnSomething = (input: readonly string[]) => {}

// Tuple: not recommended
let student:[string, number];
student=['me', 1]